import { createParticleEffect } from './particleEffectFactory.js';
export default createParticleEffect({ id:'rain', defaults:{ quantity:180, spawnRate:90, speed:900, color:'rgba(180,210,255,.72)', opacity:.55, size:1.2, length:26, wind:-140 }, spawn(e, initial){ const h=e.helpers; return { x:Math.random()*e.manager.width, y:initial?Math.random()*e.manager.height:-30, vy:h.randomBetween(.75,1.2)*(e.config.speed||900), vx:e.config.wind??-140, l:h.randomBetween(12,e.config.length||26), a:h.randomBetween(.35,1)}; }, reset(e,p){ Object.assign(p, this.spawn(e,false)); }, step(e,p,d){ p.x+=p.vx*d; p.y+=p.vy*d; }, isDead(e,p){ return p.y>e.manager.height+60 || p.x<-80 || p.x>e.manager.width+80; }, draw(e,p,ctx,o){ ctx.globalAlpha=(e.config.opacity??.55)*p.a*o; ctx.strokeStyle=e.config.color||'rgba(180,210,255,.72)'; ctx.lineWidth=e.config.size||1.2; ctx.beginPath(); ctx.moveTo(p.x,p.y); ctx.lineTo(p.x+(p.vx/p.vy)*p.l,p.y+p.l); ctx.stroke(); }});
