import { createParticleEffect } from './particleEffectFactory.js';
export default createParticleEffect({ id:'snow', defaults:{ quantity:110, spawnRate:24, speed:70, color:'#fff', opacity:.68, size:2.2, wind:18 }, spawn(e, initial){ const h=e.helpers; return { x:Math.random()*e.manager.width, y:initial?Math.random()*e.manager.height:-10, vy:h.randomBetween(.55,1.25)*(e.config.speed||70), vx:h.randomBetween(-12,12)+(e.config.wind||18), r:h.randomBetween(1,e.config.size||2.2), phase:Math.random()*6.28, a:h.randomBetween(.35,1)}; }, reset(e,p){ Object.assign(p, this.spawn(e,false)); }, step(e,p,d){ p.phase+=d; p.x+=(p.vx+Math.sin(p.phase)*18)*d; p.y+=p.vy*d; }, isDead(e,p){ return p.y>e.manager.height+20 || p.x<-30 || p.x>e.manager.width+30; }, draw(e,p,ctx,o){ ctx.globalAlpha=(e.config.opacity??.68)*p.a*o; ctx.fillStyle=e.config.color||'#fff'; ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill(); }});
