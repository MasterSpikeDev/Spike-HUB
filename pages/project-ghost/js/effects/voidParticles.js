import { createParticleEffect } from './particleEffectFactory.js';
export default createParticleEffect({ id:'voidParticles', composite:'lighter', defaults:{ quantity:120, spawnRate:20, speed:42, color:'#7c4dff', opacity:.5, size:2.3 }, spawn(e, initial){ const h=e.helpers; return { x:initial?Math.random()*e.manager.width:e.manager.width/2+h.randomBetween(-80,80), y:initial?Math.random()*e.manager.height:e.manager.height/2+h.randomBetween(-80,80), angle:Math.random()*Math.PI*2, speed:h.randomBetween(.2,1)*(e.config.speed||42), r:h.randomBetween(.6,e.config.size||2.3), life:initial?h.randomBetween(0,5):0, ttl:h.randomBetween(4,8), a:h.randomBetween(.25,1)}; }, reset(e,p){ Object.assign(p, this.spawn(e,false)); }, step(e,p,d){ p.life+=d; p.angle+=d*.35; p.x+=Math.cos(p.angle)*p.speed*d; p.y+=Math.sin(p.angle)*p.speed*d; }, isDead(e,p){ return p.life>p.ttl || p.x<-20 || p.x>e.manager.width+20 || p.y<-20 || p.y>e.manager.height+20; }, draw(e,p,ctx,o){ ctx.globalAlpha=(e.config.opacity??.5)*p.a*(1-p.life/p.ttl)*o; ctx.fillStyle=e.config.color||'#7c4dff'; ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill(); }});
