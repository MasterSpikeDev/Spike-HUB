import { createParticleEffect } from './particleEffectFactory.js';
export default createParticleEffect({ id:'embers', composite:'lighter', defaults:{ quantity:95, spawnRate:34, speed:72, color:'#ff8a2a', opacity:.72, size:2.8 }, spawn(e, initial){ const h=e.helpers; return { x:h.randomBetween(.05,.95)*e.manager.width, y:initial?h.randomBetween(.5,1)*e.manager.height:e.manager.height+20, vy:-h.randomBetween(.5,1.4)*(e.config.speed||72), vx:h.randomBetween(-35,35), r:h.randomBetween(.8,e.config.size||2.8), life:0, ttl:h.randomBetween(1.6,4.5), a:h.randomBetween(.4,1)}; }, reset(e,p){ Object.assign(p, this.spawn(e,false)); }, step(e,p,d){ p.life+=d; p.x+=p.vx*d+Math.sin(p.life*4)*18*d; p.y+=p.vy*d; }, isDead(e,p){ return p.life>p.ttl || p.y<-30; }, draw(e,p,ctx,o){ ctx.globalAlpha=(e.config.opacity??.72)*p.a*(1-p.life/p.ttl)*o; ctx.fillStyle=e.config.color||'#ff8a2a'; ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill(); }});
